[project]
name = "scenic-montage"
version = "0.1.0"
description = "Generate ~30s dramatic montage videos from YouTube clips, beat-synced to any song"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "librosa>=0.11.0",
    "numpy>=2.4.6",
]
